# Адрес API доски.
# В разработке — адрес запущенной службы.
# В бою nginx проксирует API на том же домене по пути /api, поэтому там
# указано просто /api: запросы идут на свой домен, и CORS не нужен вовсе.
VITE_API_URL=http://localhost:5081
